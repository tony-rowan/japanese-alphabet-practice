<div class="w-screen sm:h-screen p-8 sm:p-0 flex items-center place-content-evenly">
  <div class="w-11/12 grid sm:grid-cols-3 gap-8 place-content-stretch place-items-stretch">
    <% [@character.romaji, @character.hiragana, @character.katakana].each do |letter| %>
      <div class="py-16 sm:py-24 bg-white shadow-lg rounded-lg text-8xl text-center">
        <%= letter %>
      </div>
    <% end %>
  </div>
</div>
